
close(f);
f = figure;
% targetIdx = [16];
% plot_subtree(boundaries, parent, targetIdx, sampleE);
% newBoundaries = merge_boundaries(boundaries, parent, targetIdx, sampleE);
% plot_bound(newBoundaries, sampleE);
plot_yinset(result, [8]);

% result = build_yinset(F, V, curves, E);

axis([.1 .9 .1 .9]);